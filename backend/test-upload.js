import fs from 'fs'; const BUBBLE_API_URL = 'https://site2app-34905.bubbleapps.io/version-test/api/1.1/obj'; const BUBBLE_API_TOKEN = '59ef5eb57d786ff8eced03244342f32e'; async function t() { const fd = new FormData(); const blob = new Blob([Buffer.from('abcd')], { type: 'application/vnd.android.package-archive' }); fd.append('file', blob, 'test.apk'); const res = await fetch(BUBBLE_API_URL.replace('/obj', '/wf') + '/upload_apk', { method: 'POST', headers: { 'Authorization': 'Bearer ' + BUBBLE_API_TOKEN }, body: fd }); console.log(res.status, await res.text()); } t();
